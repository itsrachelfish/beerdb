{{> head}}
    {{> sidebar}}

    <h2>
        Viewing Table: {{table.name}}

        <div class="pull-right">
            <a href="/insert/{{table.url}}" class="btn btn-success add-column">Add Row</a>
            <a href="/search/{{table.url}}" class="btn btn-primary">Search Table</a>
        </div>
    </h2>
    
    <table class="table table-striped">
        <thead>
            <tr>
                {{#columns}}
                    <th>{{name}}</th>
                {{/columns}}
            </tr>
        </thead>

        <tbody>
            {{#rows}}
                <tr>
                    {{#sortable}}
                        <td>{{value}}</th>
                    {{/sortable}}
                </tr>

                <tr>
                    <td colspan="{{sortable.length}}">
                        {{#text}}
                            <p>
                                <b>{{name}}</b> - {{value}}
                            </p>
                        {{/text}}
                    </td>
                </td>
            {{/rows}}
        </tbody>
    </table>

    <div class="pull-right">
        <a href="/insert/{{table.url}}" class="btn btn-success add-column">Add Row</a>
        <a href="/search/{{table.url}}" class="btn btn-primary">Search Table</a>
    </div>
{{> foot}}
